
{{!-- <h1 class="mb-1 restaurant-show-title">
  {{ restaurant.name }}
  <span class="badge badge-pill badge-danger font-weight-normal">
    {{ restaurant.rating }}
    <i class="fas fa-star fa-xs"></i>
  </span>
</h1> --}}
<div class="container">
  <div class="row">
    <div class="col-12 col-md-10 mx-auto">
      <div class="row">
        <h1 class="mb-1 restaurant-show-title mx-auto text-primary">
          {{ restaurant.name }}
          {{!-- <span class="badge badge-pill badge-danger font-weight-normal">
            {{ restaurant.rating }}
            <i class="fas fa-star fa-xs"></i>
          </span> --}}
        </h1>
      </div>
      <div class="row justify-content-between">
        <div class="col-auto mb-4">
          <a class="btn btn-warning" href="/" role="button">
            <i class="fas fa-arrow-left mr-2"></i>回首頁
          </a>
        </div>
        <div class="col-auto mb-4">
          <a class="btn btn-success" href="/restaurants/{{ restaurant._id }}/edit" role="button">
            <i class="fas fa-edit mr-2"></i>編&nbsp;輯
          </a>
          <form action="/restaurants/{{ todo._id }}?_method=DELETE" method="POST" style="display: inline;">
            <button class="btn btn-danger" data-toggle="modal" data-target="#alertModal{{ restaurant._id }}">
              <i class="fas fa-trash-alt mr-2"></i>刪&nbsp;除
            </button>
          </form>
        </div>
      </div>
      <h6 class="mb-1">
        <span class="badge badge-pill badge-info font-weight-normal">
          <i class="fas fa-utensils pr-2"></i> {{ restaurant.category }}
        </span>
        <span class="badge badge-pill badge-warning font-weight-normal">
          {{ restaurant.rating }}
          <i class="fas fa-star fa-xs"></i>
        </span>
      </h6>

      <p class="mt-2 mb-1">
        <span class="text-dark">
          <i class="fas fa-map-marker-alt pr-2"></i>
          地址：
        </span>
        {{ restaurant.location }}
        <a href="{{ restaurant.google_map }}" class="text-secondary" target="_blank">
          <i class="fas fa-location-arrow pr-2 fa-xs"></i>
        </a>
      </p>

      <p class="mb-1">
        <span class="text-dark">
          <i class="fas fa-mobile-alt pr-2"></i>
          電話：
        </span>
        {{ restaurant.phone }}
      </p>

      <p class="mb-5">
        {{ restaurant.description }}
      </p>

      <img class="rounded mx-auto d-block mb-4 w-100" src="{{ restaurant.image }}" alt="{{ restaurant.name }}" style="max-width: 600px;">
    </div>
    <!-- Modal -->
    <div class="modal fade" id="alertModal{{ restaurant._id }}" tabindex="-1" role="dialog" aria-hidden="true">
      <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
          <div class="modal-header border-0 pb-0">
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body text-center pt-0">
            <div class="text-warning" style="font-size:70px;">
              <i class="fas fa-exclamation-circle"></i>
            </div>
            <h5>您確定要刪除 <span class="text-info">{{ restaurant.name }}</span> 資料嗎？</h5>
          </div>
          <div class="modal-footer border-0 text-center inline-block mx-auto">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">取消</button>
            <form action="/restaurants/{{restaurant._id}}/delete" method="POST">
              <button type="submit" class="btn btn-success">確定</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>